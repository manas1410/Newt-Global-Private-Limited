Code -
        DECLARE
  v_name   VARCHAR2(50);
  v_salary employees.salary%type;
BEGIN
  SELECT first_name ||' '|| last_name, salary 
  INTO   v_name, v_salary  
  FROM   employees 
  WHERE  employee_id = 7;
  dbms_output.put_line('The salary of '|| v_name || ' is : '|| v_salary);
END;


Output - 
Statement processed.
The salary of First7 Last7 is : 76840.5


Code -DECLARE
  v_employee_id     PLS_INTEGER := 0;
  v_salary_increase NUMBER      := 400;
BEGIN
  FOR i IN 11..20 LOOP
    INSERT INTO employees_copy 
    (employee_id, first_name, last_name, salary)
    VALUES
    (i, 'employee#'||i,'temp_emp',1000);
    -- UPDATE employees_copy 
    -- SET    salary = salary + v_salary_increase
    -- WHERE  employee_id = i;
    -- DELETE FROM employees_copy
    -- WHERE employee_id = i;
  END LOOP;
END;


Output -Statement processed.








Code - 
CREATE SEQUENCE employee_id_seq
START WITH 207
INCREMENT BY 1;


BEGIN
  FOR i IN 1..10 LOOP
    INSERT INTO employees_copy 
      (employee_id,first_name,last_name,salary)
    VALUES 
      (employee_id_seq.nextval,'employee#'||employee_id_seq.nextval,'temp_emp',1000);
  END LOOP;
END; 


Output - Statement processed.


Code -
DECLARE
  v_seq_num NUMBER;
BEGIN
  SELECT employee_id_seq.nextval 
  INTO   v_seq_num 
  FROM   dual;
  dbms_output.put_line(v_seq_num);
END;


Output -
Statement processed.
41
Code -
DECLARE
  v_seq_num NUMBER;
BEGIN
  SELECT employee_id_seq.nextval 
  INTO   v_seq_num 
  FROM   employees_copy 
  WHERE  rownum = 1;
  dbms_output.put_line(v_seq_num);
END;


Output -


Statement processed.
42
Code -
DECLARE
  v_seq_num NUMBER;
BEGIN
  v_seq_num := employee_id_seq.nextval; 
  dbms_output.put_line(v_seq_num);
END;


Output -
Statement processed.
43


Code -
Output -
Code -
Output -
Code -
Output -
Code -
Output -
Code -
Output -
Code -
Output -
________________
Code -
Output -